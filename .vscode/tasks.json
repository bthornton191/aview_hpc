{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "freeze_main",
            "command": "${workspaceFolder}\\env\\Scripts\\activate.bat & ${workspaceFolder}\\freeze.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
